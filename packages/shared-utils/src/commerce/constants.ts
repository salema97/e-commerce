/** Standard Ecuador IVA rate (15%). */
export const ECUADOR_IVA_RATE = 0.15 as const;

/** Default free-shipping threshold in USD when env is unavailable. */
export const DEFAULT_FREE_SHIPPING_THRESHOLD = 50;

/** Default flat shipping rate in USD when env is unavailable. */
export const DEFAULT_SHIPPING_FLAT_RATE = 5;
